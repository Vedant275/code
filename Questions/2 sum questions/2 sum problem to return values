import java.util.*;

/**
 * Q.What if we don't print but need the values / indices in return from the function.
 *
 * In this question we need to return both the values from the function instead of
 * just printing them (using print command)
*/

public class Main {
  static int[] twoSum(int[] arr, int v) {
    // Hashmap created to store the value of number and against its index
    HashMap<Integer, Boolean> map = new HashMap<>();
    
    for (int i = 0; i < arr.length; i++) {
      int requiredNumber = v - arr[i];
      // Check if the requiredNumber already exists
      if (map.containsKey(requiredNumber)) {
        return new int[]{requiredNumber, arr[i]};
      }
      // Make the return value ready as a pair
      map.put(requiredNumber, true);
    }
    return new int[]{-1, -1};
  }

  public static void main(String[] args) {
    System.out.println("Enter the number of the values in the array");
    Scanner sc = new Scanner(System.in);

    int n = sc.nextInt();
    int[] arr = new int[n];
    for (int i = 0; i < n; i++) {
        System.out.println("Enter the number: " + (i + 1));
        arr[i] = sc.nextInt();
    }

    int[] result = twoSum(arr, 6);

    System.out.println(result[0] + " " + result[1]);
  }
}
